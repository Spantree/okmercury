class LoginFilters {
	 def filters = {
//        loginCheck(controller: '*', action: '*') {
//            before = {
//                if (!session.user && !['login', 'register', 'created'].contains(actionName)) {
//                    redirect(uri: '/login')
//                    return false
//                }
//            }
//        }
    }
}